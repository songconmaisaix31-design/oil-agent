# C1 display-only preparation handoff

This is local preparation only. The application has **not been created**.
`油品预警助手（测试）` and `oil-agent-feishu-trial` are chosen labels, not app,
tenant or user identities. Product Feishu/source/model requests and paid product
cost in this work: **0**. No login, callback, phone receipt or start authorization
has been executed or generated.

## Local preview

Open [the self-contained preview](../../../web/c1-preview.html) directly in a
browser, without Vite or an API. It displays the exact fixed title/body, prominent
`演练／非真实行情`, a system-generated test number and generation time, ending in
`本阶段只验证消息到达。`. It contains no links, buttons, scripts or remote assets;
CSP forbids network resources. Generation time is not a send timestamp.
This HTML projection is not a screenshot of the Feishu client.

[The adjacent JSON](../../../web/c1-preview.json) contains `msg_type` and the
serialized `content` of the same card. Preview code commit:
`584c9a3b49c549a4602bc32b0a29d0e7261d7425` (`[skip ci]`).

Exports from `oil_agent.channels`:

```python
build_c1_card(*, test_id: str, created_at: datetime) -> dict
create_c1_preview(*, test_id: str | None = None,
                  created_at: datetime | None = None) -> dict[str, str]
```

The preview result contains `test_id`, UTC ISO `created_at`, `msg_type`, `content`
and `html`. Defaults generate `c1-` plus UUID4 hex and the current aware UTC time;
explicit inputs allow C to retain the same metadata. The functions perform no I/O
and create no permission or delivery. `C1_TITLE`, `C1_BODY`, `C1_DATASET` are
exported constants. C alone may write the returned HTML/content through its fixed
private helper under the authorized private directory; D never reads or writes
that directory, IDs or secrets. This is not a second configuration form.

## Reproduced blockers and smallest owner seams

At the adopted I `3a5591a9fd6f8d76a6a209e4ec9a780496a773ce` baseline:

- `channels/cards.py:47-52`: empty `public_base_url` raises
  `ServiceError: A configured HTTPS URL is required`; supplying a URL produces
  `open_url` and `callback` behaviors. This cannot represent the approved C1 card.
- `channels/feishu.py:56-72`: no per-HTTP request reservation callback exists;
  the token request at line 78 occurs outside the runtime's per-message budget.
- `bootstrap.py:114-166` (I-owned): disabled identity skips channel wiring;
  line 121 requires `OIL_FEISHU_REDIRECT_URI`, lines 147-148 require callback
  encryption/verification secrets. A mocked missing-field resolver reproduces
  the redirect error without credentials/network. D did not edit bootstrap.
- C identified that using event/first_report for this generic delivery test
  requires fabricated market assessment. The approved contract is instead an
  explicit `subject_type=exercise`, `kind=exercise` in C's existing durable path.
  The C1 renderer must reject ordinary event/report intents, including fixture
  first reports; it must not manufacture source evidence or rule approvals.

I's C1 assembly uses the existing `FeishuChannel`, with `c1_display_only=True`,
one server-owned test-recipient mapping, existing recipient authorization and the
mandatory `authorize_request=runtime.authorize_c1_request` hook returning
`Awaitable[str]`. Operations are exactly `tenant_token` and `message_send`;
C returns a durable reservation ID or
raises before the HTTP effect. Token cache hits make no HTTP request. Explicit
subsequent attempts reserve again; there are no automatic retries or queries.
The hook covers every implemented C1 Feishu API request, not just message POST.
Response loss remains `UNKNOWN` without resend; expiry/quota/revocation remain
separate. C owns the start window, outbox/deduplication, per-attempt/send/request
budgets and revocation. Preview never opens the future window.

`FeishuSettings` still requires explicit enabled/app_id/tenant_key/app_secret for
sending. C1 requires no public URL, OAuth redirect, callback secrets, identity
adapter or verifier. The ordinary renderer keeps HTTPS/detail/callback behavior,
and refuses exercise content without explicit C1 mode. I owns that limited
factory branch; no direct/raw sender is introduced. The verified channel commit
is recorded in the completion handoff.

```python
FeishuChannel(
    feishu_settings,
    recipients={recipient_id: FeishuRecipient(open_id, is_test_recipient=True)},
    authorize=runtime.authorize_recipient,
    c1_display_only=True,
    authorize_request=runtime.authorize_c1_request,
)
```

All arguments above come from C's validated private configuration and exact
single-person permission, not from the browser. The approved C DTO baseline is
`fda37c87719c4ef9840fe866308707314c3832e2`, normally merged before channel
verification. C/I still own durable runtime-hook and factory assembly acceptance;
the transport unit tests use mocked authorization and HTTP, not a live runtime.

## Exact next application-console steps (not executed)

Official docs checked 2026-09-12:
[custom app creation walkthrough](https://open.feishu.cn/document/uAjLw4CM/uMzNwEjLzcDMx4yM3ATM/how-to-call-a-server-side-api/introduction),
[application development](https://open.feishu.cn/document/home/introduction-to-custom-app-development/self-built-application-development-process),
[send API and permissions](https://open.feishu.cn/document/server-docs/im-v1/message/create).

1. The user's next click in the [developer console](https://open.feishu.cn/app)
   is **创建企业自建应用**. Enter the name **油品预警助手（测试）**, a description
   identifying a personal delivery drill, then **创建**. A Feishu enterprise
   membership is a documented prerequisite; possession of a personal account
   alone does not establish it. No app was created during this task.
2. In that new app, choose **应用能力 → 添加应用能力 → 按能力添加 → 机器人 → 添加**.
3. In **权限管理**, request only **以应用的身份发消息**
   (`im:message:send_as_bot`) for this display-only test. Enable/publish the bot
   and permission changes as required by the tenant's review process. Restrict
   app availability to the one intended test person. Do not use the API debug
   console's sender, add group recipients, or configure OAuth/callbacks for C1.
4. The user supplies the resulting actual app/tenant/recipient binding only to
   C's protected project file; secret values stay out of chat, Git and logs.
   Labels and documentation examples must never stand in for platform IDs.

Sending remains disabled until exact identity/host/cost coverage is confirmed and
the user explicitly says **开始手机测试**. The future authorization is 30 minutes,
first send one message, at most three send attempts and twenty total Feishu API
requests (including token/query/retry if ever implemented), added fee zero.
Unknown cost or delivery outcome stops execution; no next window is implicit.
Phone observation is recorded separately from platform acceptance. Login/ack and
production acceptance remain outside C1.

## Focused verification

- `uv run --locked pytest tests/unit/channels/test_c1.py tests/unit/channels/test_c1_preview.py -q`:
  **24 passed** with normal construction of C's actual `NotificationIntent`.
  Fixed exercise isolation, preview/HTTP payload equality, exact single-recipient
  configuration, reservation before each token/send request, token-cache and
  expiry counts, gate denial/timeout, revocation and UNKNOWN/no retry are covered.
- `uv run --locked pytest tests/unit/channels/test_channels.py -q -k 'card or fixture or default_off or acceptance_has or transport_loss or classified_provider or revocation or expired_token or trial_recipient or malformed_recipient'`:
  **40 passed, 30 deselected**, preserving existing ordinary-flow assertions.
- `uv run --locked ruff check src/oil_agent/channels/c1.py src/oil_agent/channels/cards.py src/oil_agent/channels/feishu.py tests/unit/channels/test_c1.py`:
  passed; scoped Ruff format and Prettier checks passed.
- `node web/scripts/c1-preview-check.mjs`: exact card-text projection, 390/320px
  without overflow, no actions, network requests or page errors; isolated
  headless Chrome was closed. No dev server, Docker or public ingress was started.

No repeated full acceptance, frontend suite/build or PostgreSQL run was needed
for these renderer/transport seams and standalone HTML; C/I/E verify the durable
assembly independently. Missing application/identity/host and explicit start are
authorization/configuration gaps. Real platform acceptance and phone receipt are
not executed, and this preview is not evidence of either.
