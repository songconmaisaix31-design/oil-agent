# Deliberately empty public Compose env file: prevents automatic .env discovery.
# Inject newly authorized OIL_DATABASE_URL and OIL_POSTGRES_PASSWORD in the process.
# Never put credential values in this file or print rendered Compose configuration.
